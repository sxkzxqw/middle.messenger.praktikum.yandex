{{#> Modal extraClass=extraClass ref='modal'}}
{{{ CloseModalButton ref='close_button' onClick=closeModal }}}
<form class="form__change-password">
  <h1 class="title text-center register__title">{{title}}</h1>
  {{{ Button type='submit' variant=extraClassButton to='/profile' text=buttonText }}}
</form>
{{/Modal}}